import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    errorContext: {
        alignItems: 'center'
    },
    errorText: {
        fontSize: 20,
        marginTop: 15
    }
})

export default styles